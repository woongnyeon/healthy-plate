spring:
  datasource:
    url: jdbc:mysql://localhost:3306/healthy_plate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: healthyplate
    password: healthyplate123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 999984500156-f0jjsrc0enut8thj37o0tpi9lmqaohkr.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile
              - email
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

jwt:
  secret-key: hAPm,;DVGw|IM>PR)d{>WRGJeARkCS*_V61G4)t<xl^
  access-token-expiration: 900000      # 15분
  refresh-token-expiration: 604800000  # 7일

app:
  frontend:
    url: http://localhost:5173
  auth:
    success-url: http://localhost:5173/login
    failure-url: /login?error=true
  cookie:
    secure: false  # 로컬 개발환경에서는 HTTP 사용
